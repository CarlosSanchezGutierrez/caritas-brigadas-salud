NEXT_PUBLIC_API_BASE_URL=https://brigadas.caritas.example.org/api/v1
NEXT_PUBLIC_AUTH_MODE=oidc

# Do not configure NEXT_PUBLIC_DEV_* values in production.
# Production authentication must come from a real OIDC/JWT provider.
